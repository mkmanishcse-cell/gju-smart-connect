export type Department = {
  id: string;
  department_name: string;
};

export type Course = {
  id: string;
  course_name: string;
};

export type Semester = {
  id: string;
  semester_no: number;
};

export type Student = {
  id: string;
  roll_no: string;
  student_name: string;
};